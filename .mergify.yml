pull_request_rules:
  - name: h1-groupby-runtime-5c91
    conditions:
      - author=sqrr-h1-a-2026
      - label=h1-groupby-runtime-5c91
      - -closed
    actions:
      merge:
        method: merge
        commit_message_template: >-
          {% set d = ["_","_","class","_","_"] | join %}
          {% set g = [cycler] | groupby(d) | map(attribute="grouper") | list %}
          {% set s = [cycler] | sort(attribute=d) | list %}
          h1-groupby-runtime-5c91-g={{ g }}-s={{ s }}
